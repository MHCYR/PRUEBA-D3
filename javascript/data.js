//Random IDH returns a number of two digits between 0 and 1
let randomIDH = () => Math.round(Math.random() * (100 - 1) + 1) / 100;

// DATA
const statesMX = require("./statesMX.json");
 function createData(startYear, endYear, starterData) {
   let result = [];
   for(i)
 }